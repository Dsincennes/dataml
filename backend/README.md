1. Install python
2. make python env in terminal (python -m venv env)
3. activate in terminal (./env/Scripts/activate IIRC)
4. pip install -r requirements.txt

### Start Backend Service
uvicorn main:app --reload